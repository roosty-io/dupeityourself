# AI provider configuration (all optional - app runs fully in mock mode without keys)
# AI_PROVIDER: "anthropic" | "openai" | "mock"
AI_PROVIDER=mock
USE_MOCK_AI=true
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Model used when AI_PROVIDER=anthropic
ANTHROPIC_MODEL=claude-sonnet-5

# Server
PORT=3001
